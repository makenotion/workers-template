name: Check agent configs

on:
  push:
    branches: [main]
  pull_request:

jobs:
  agents-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: .agents/sync.sh --check
